{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", ".", "--allow-write"],
      "description": "File system access for document management and analysis"
    }
  },
  "recommendedServers": {
    "docker-pdf-extraction": {
      "command": "docker",
      "args": ["run", "-it", "--rm", "-v", "${PWD}:/workspace", "mcp-pdf-extraction"],
      "description": "Advanced PDF processing with OCR capabilities",
      "category": "document-processing",
      "priority": "high",
      "useCase": "Document analysis, evidence extraction, legal document processing"
    },
    "docker-statistical-analysis": {
      "command": "docker", 
      "args": ["run", "-it", "--rm", "-v", "${PWD}:/workspace", "mcp-statistical-analysis"],
      "description": "Statistical analysis and data visualization",
      "category": "data-analysis",
      "priority": "high",
      "useCase": "Financial modeling, market research, performance analytics"
    },
    "docker-research-database": {
      "command": "docker",
      "args": ["run", "-it", "--rm", "mcp-research-database"],
      "description": "Academic and professional research database access",
      "category": "research",
      "priority": "medium",
      "useCase": "Literature review, case law research, competitive intelligence"
    },
    "web-search-enhanced": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-web-search"],
      "description": "Enhanced web search with verification capabilities",
      "category": "research",
      "priority": "medium",
      "useCase": "Real-time information gathering, fact verification, market intelligence"
    }
  },
  "specializedConfigurations": {
    "legal-analysis": {
      "servers": ["filesystem", "docker-pdf-extraction", "docker-research-database"],
      "description": "Configuration optimized for legal document analysis and case research",
      "additionalServers": {
        "legal-database-connector": {
          "command": "docker",
          "args": ["run", "-it", "--rm", "mcp-legal-database"],
          "description": "Legal database and case law access"
        },
        "citation-manager": {
          "command": "docker",
          "args": ["run", "-it", "--rm", "mcp-citation-manager"],
          "description": "Legal citation formatting and verification"
        }
      }
    },
    "business-intelligence": {
      "servers": ["filesystem", "docker-statistical-analysis", "web-search-enhanced"],
      "description": "Configuration for market research and business analysis",
      "additionalServers": {
        "market-data-connector": {
          "command": "docker",
          "args": ["run", "-it", "--rm", "mcp-market-data"],
          "description": "Real-time market data and financial information"
        },
        "business-database": {
          "command": "docker",
          "args": ["run", "-it", "--rm", "mcp-business-intelligence"],
          "description": "Business intelligence and competitive analysis"
        }
      }
    },
    "technical-development": {
      "servers": ["filesystem", "web-search-enhanced"],
      "description": "Configuration for software development and technical analysis",
      "additionalServers": {
        "code-analysis": {
          "command": "docker",
          "args": ["run", "-it", "--rm", "-v", "${PWD}:/code", "mcp-code-analysis"],
          "description": "Code quality analysis and security scanning"
        },
        "performance-monitoring": {
          "command": "docker",
          "args": ["run", "-it", "--rm", "mcp-performance-monitor"],
          "description": "Application performance monitoring and optimization"
        }
      }
    },
    "academic-research": {
      "servers": ["filesystem", "docker-research-database", "docker-statistical-analysis"],
      "description": "Configuration for academic research and literature analysis",
      "additionalServers": {
        "arxiv-connector": {
          "command": "docker",
          "args": ["run", "-it", "--rm", "mcp-arxiv"],
          "description": "ArXiv academic paper access and analysis"
        },
        "pubmed-connector": {
          "command": "docker",
          "args": ["run", "-it", "--rm", "mcp-pubmed"],
          "description": "PubMed medical research database access"
        }
      }
    }
  },
  "installationInstructions": {
    "docker": {
      "prerequisites": [
        "Docker Desktop installed and running",
        "Sufficient disk space for container images (minimum 5GB)",
        "Network access for container downloads"
      ],
      "commands": [
        "docker pull mcp-pdf-extraction",
        "docker pull mcp-statistical-analysis", 
        "docker pull mcp-research-database"
      ]
    },
    "npm": {
      "prerequisites": [
        "Node.js v18+ installed",
        "NPM or Yarn package manager",
        "Network access for package downloads"
      ],
      "commands": [
        "npm install -g @modelcontextprotocol/server-filesystem",
        "npm install -g @modelcontextprotocol/server-web-search"
      ]
    }
  },
  "troubleshooting": {
    "common_issues": {
      "docker_not_running": {
        "symptom": "Docker MCP servers fail to start",
        "solution": "Ensure Docker Desktop is installed and running",
        "command": "docker --version && docker ps"
      },
      "permission_denied": {
        "symptom": "File system access denied",
        "solution": "Check file permissions and run with appropriate access",
        "command": "chmod -R 755 /path/to/workspace"
      },
      "container_not_found": {
        "symptom": "Docker container not found",
        "solution": "Pull the required container image",
        "command": "docker pull [container-name]"
      }
    },
    "performance_optimization": {
      "memory_usage": "Limit concurrent MCP servers based on available RAM",
      "disk_space": "Regular cleanup of temporary files and container images",
      "network": "Use local containers when possible to reduce network dependency"
    }
  },
  "configuration_examples": {
    "claude_config": {
      "file_path": "~/.claude/mcp-servers.json",
      "example": {
        "mcpServers": {
          "filesystem": {
            "command": "npx",
            "args": ["-y", "@modelcontextprotocol/server-filesystem", ".", "--allow-write"]
          },
          "pdf-processor": {
            "command": "docker",
            "args": ["run", "-it", "--rm", "-v", "${PWD}:/workspace", "mcp-pdf-extraction"]
          }
        }
      }
    }
  }
}